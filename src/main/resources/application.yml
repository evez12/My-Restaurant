spring:
  app:
    jwtSecret: MySecretHGYvvTfsg6352432hhdgshgFFwtfsaFAsgg323vxhEg
  application:
    name: RestaurantMS
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurant_db
    username: evez
    password: evez
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  #    open-in-view: false
  main:
    banner-mode: off


#Logging Configuration
logging:
  level:
    root: info
    org:
      springframework:
        web: debug
        security: debug
      authentication: debug
      hibernate: warn
    com:
      huseynov:
        restaurant: info
    include-aplication-group: false
    inculde-aplication-name: false

# My values
api:
  prefix: /api/v1
frontend:
  url: http://localhost:3000

## swagger configuration
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html